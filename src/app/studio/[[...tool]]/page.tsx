'use client';

/**
 * Sanity Studio route.
 * This will be configured once Sanity Studio is fully set up.
 */
export default function StudioPage() {
  return (
    <div className="flex min-h-screen items-center justify-center bg-white text-black">
      <p>Sanity Studio will be configured here.</p>
    </div>
  );
}
